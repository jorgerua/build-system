# Buildah registries configuration for local development.
# Marks the local registry:2 service as insecure (HTTP, no TLS).
# Mounted into the worker container at /etc/containers/registries.conf.

[[registry]]
location = "registry:5000"
insecure = true
